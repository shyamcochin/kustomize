apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../resources

buildMetadata: [managedByLabel, originAnnotations]
commonLabels:
  application: coit-frontend
  webserver: nginx
  location: mumbai

patches:
  - path: coit-frontend-deployment-patch.yaml